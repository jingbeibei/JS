ngInclude
将其它HTML（模板）文件包含在主页面中
ngInclude指令会使用XHR下载指定的文件，
编译其中的ng指令和模板插值{{}}，并将
编译后的子视图插入到主视图中。

在ngInclude包含进来的子视图中，可以使用
ng-controller对子视图进行单独的控制。

但是ngInclude包含的文件中不能有<script>标签，
即使加入script标签，其脚本也不会被下载执行，
如果希望通过控制器单独控制子视图，则控制器
部分的代码应该在主页面中提前加载。

